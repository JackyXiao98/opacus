# Requirements for Single GPU Memory Profiler
# Base requirements (should already be available in opacus environment)
torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0
huggingface_hub>=0.15.0
peft>=0.4.0

# Additional requirements for memory profiling
matplotlib>=3.5.0
seaborn>=0.11.0
pandas>=1.5.0
numpy>=1.21.0
tqdm>=4.64.0

# Optional: for enhanced profiling
psutil>=5.9.0  # for system memory monitoring
gpustat>=1.1.0  # for GPU monitoring

# Note: opacus should be installed from the parent directory
# pip install -e ../../